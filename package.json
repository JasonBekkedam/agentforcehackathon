{
	"name": "tableau-react-extension",
	"version": "1.0.0",
	"scripts": {
		"start": "node server.js",
		"heroku-postbuild": "npm install --prefix client && npm run build --prefix client"
	},
	"dependencies": {
		"express": "^4.18.2"
	}
}
